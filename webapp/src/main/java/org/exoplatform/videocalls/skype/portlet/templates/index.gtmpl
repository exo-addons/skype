<script type="text/javascript">
	if (typeof videoCallsEnv === "undefined" || !videoCallsEnv) {
		videoCallsEnv = {};
	}
	videoCallsEnv.skype = {
		name : "${name}",
		title : "${title}",
		callTitle : "${callTitle}",
		clientId : "${clientId}",
		redirectUri : "${redirectUri}",
		origins : ["https://webdir.online.lync.com/autodiscover/autodiscoverservice.svc/root"],
		version : "${version}",
		apiKey : "${apiKey}",
		apiKeyCC : "${apiKeyCC}"
	};
</script>